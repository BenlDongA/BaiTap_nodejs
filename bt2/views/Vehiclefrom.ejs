<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chọn Thành Phố</title>
    
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/4.5.2/css/bootstrap.min.css">
    <link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>   
    <script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js"></script>
    
    <style>
        /* Điều chỉnh chiều rộng tối đa cho form */
        .form-container {
            max-width: 600px;
            margin: 0 auto; /* Canh giữa theo chiều ngang */
        }

        /* Đảm bảo dropdown và các phần tử liên quan vừa với form */
        .select2-container .select2-selection--single {
            height: calc(1.5em + 0.75rem + 2px);
        }

        /* Đảm bảo canh giữa theo chiều dọc */
        .full-height {
            min-height: 100vh; /* Chiều cao toàn màn hình */
        }
    </style>
</head>
<body>
    <div class="container full-height d-flex justify-content-center align-items-center">
           <div class="nav-item">
                <a class="nav-link" href="/">Home</a>
            </div>
        <div class="form-container">
        
            <h1>TRA CỨU BIỂN SỐ XE Ở VIỆT NAM  <i class="bi bi-house"></i></h1>
            <div class="form-group">
                <label for="city">Thành Phố</label>
                <select id="city" name="city" class="form-control" required onchange="updatePlate()">
                    <option value="" disabled selected>
                        <%= selectedCity ? selectedCity : 'Chọn thành phố' %>
                    </option>
                    <% cities.forEach(function(city) { %>
                        <option value="<%= city.city %>" <%= (selectedCity === city.city) ? 'selected' : '' %>><%= city.city %></option>
                    <% }); %>
                </select>
            </div>
            <% if (plates) { %>
                <div class="mt-4">
                    <h2>Biển Số Xe: <%= plates %></h2>
                </div>
            <% } %>
        </div>
    </div>
    <script>
        $(document).ready(function() {
            $('#city').select2({
                placeholder: "Chọn thành phố",
                allowClear: true,
                width: '100%'
            });
        });

        function updatePlate() {
            const citySelect = document.getElementById('city');
            const selectedCity = citySelect.value;
            if (selectedCity) {
                window.location.href = `/vehicle?city=${encodeURIComponent(selectedCity)}`;
            }
        }
    </script>
</body>
</html>
